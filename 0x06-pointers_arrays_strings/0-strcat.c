#include "main.h"
/**
 * _strcat  - pointer functio that concatenates two strings
 * @dest: pointer destination string
 * @src: pointer source string
 * Return: String
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
