#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strcpy -  concatenates string
 * @dest: array destination
 * @src : source array
 * Return: string
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
